// v0-baseline — the measurable floor.
//
// Strategy: classify creeps by body part on tick 1; one main squad rushes the
// enemy flag; one healer is held back as a flag sentry; per-creep micro is
// "shoot what's in range, kite if too close, heal whoever needs it most".
//
// This is intentionally weak. Its job is to be the comparison baseline for
// everything that follows. Do not edit this file to "improve" it — fork a
// new variant instead, so we keep a stable reference point.

import { tickV0 } from "../src/commander/commander.mjs";

export function loop() {
  tickV0();
}
